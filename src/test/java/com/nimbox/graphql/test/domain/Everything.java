package com.nimbox.graphql.test.domain;

import com.nimbox.graphql.annotations.GraphQLUnion;

@GraphQLUnion(name = "Everything", description = "Everything one can be.")
public interface Everything {

}
